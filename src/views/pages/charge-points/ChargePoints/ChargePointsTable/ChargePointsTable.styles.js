import { styled } from '@mui/styles';
import { Link } from 'react-router-dom';

export const ElementName = styled(Link)(() => ({
    fontWeight: 500,
    color: 'rgb(33, 33, 33)',
    textDecoration: 'none'
}));
